Frontend - данная версия не предусматривает работу с админ частью фронта

1. Настройка глобального состояния

- Создать базовый store в store/ с подключением слайсов: productsSlice, ingredientsSlice, storesSlice, ordersSlice.

- Настроить API slice для взаимодействия с сервером (/api/products, /api/stores, /api/orders, /api/ingredients).

2. Организация структуры страниц

- Главная страница (App.tsx) — Route HomePage для обычных пользователей, AdminPage - для админа

- HomePage - отображение всех категорий продуктов с блоками и карточками товаров. (Хочешь я подготовлю сразу базовый Home.tsx, где товары будут выводиться блоками по категориям с использованием useGetProductsQuery)

- AdminPage - форма входа для администратора

- Создать логику каждого “среза”: products/, orders/, stores/, ingredients/. в нутри /features

3. Компоненты UI

- Карточки продуктов (с кнопкой “Добавить в корзину” и кнопкой "Ингредиенты").

- Модальное окно для возможномти добавления/удаления ингридиентов, пересчет стоимости + добавление в корзину.

- Модальное окно корзины 
	- список товаров с возможностью редактирования
	- итоговая сумма
	- к оформлению закза(страница CheckoutPage)

- Модальное окно формы обратной связи

Использовать shadcn/ui для клиентской части, MUI админка.

4. Асинхронные действия

- Сделать загрузку данных через RTK Query (products, ingredients, stores).

- Настроить отображение состояния загрузки и ошибок.

5. Работа с формами

- Подключить React Hook Form + yup для валидации на фронте (пока только валидация страницы создания заказа + форма обратной связи).



---------------------------------------------------------------------

- Интегрировать с Joi через схемы, чтобы фронт и бэк были синхронизированы. (для админки)

- Сделать UI для админки (MUI):

- управление товарами, ингредиентами, заказами

- Сделать UI для клиента (shadcn/ui):

- каталог продуктов с фильтрацией

- корзина и оформление заказа

- выбор заведения (store) по адресу или самовывоз

- настроить админ-функции: добавление и редактирование

- const BASE_URL =
  (import.meta as any).env?.VITE_API_URL ?? "http://localhost:5000/api"; - я так понимаю гадо все адреса через .env заменить

